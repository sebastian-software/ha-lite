{
  "_comment": [
    "Input for script/ha_lite_closure.py.",
    "",
    "'roots' are the anchors the retained runtime is defined by. They mirror the",
    "job matrices in .github/workflows/ha-lite-ci.yml: what CI protects is what",
    "ha-lite promises to keep working.",
    "",
    "'device_class_semantics' roots are the components that define the named",
    "trigger and condition vocabulary over entity device classes (motion.detected,",
    "door.opened, air_quality.smoke_detected). binary_sensor and sensor ship no",
    "trigger.py or condition.py of their own, so these components are the only",
    "semantic layer over them. See docs/adr/0012.",
    "",
    "'aggregation' roots fold several retained entities into one derived answer:",
    "person over device_tracker, group over any entity domain. Nothing imports",
    "them, so they need root status or Wave 4 deletes them by reachability.",
    "Aggregation states what is, not what should happen. See docs/adr/0002.",
    "",
    "'environment' roots observe the physical surroundings rather than a device.",
    "sun computes solar position from the configured coordinates and the clock.",
    "astral and helpers/sun.py are core already, so the component only exposes",
    "what the core computes anyway. Fronius makes it device-relevant: solar",
    "elevation is what a PV site's yield follows. See docs/adr/0002.",
    "",
    "'test_fixtures' roots are the simulated devices retained upstream suites",
    "set up: demo as a stand-in domain and as the platform behind the",
    "media_player, group, camera and other tests; kitchen_sink behind group's",
    "lock tests. Their platforms for domains ha-lite does not retain are",
    "pruned, so they add nothing to the closure but themselves.",
    "",
    "Every component in the tree outside the closure is the catalog: upstream",
    "integrations kept as they are. They are not roots, so the closure does not",
    "grow with them, and CI runs their suites in the catalog job.",
    "",
    "'excluded' lists the product layers ha-lite removed on purpose, grouped by",
    "what they served. One of them appearing in the tree again is a finding.",
    "The reasons are in docs/architecture/scope-matrix.md. An integration that",
    "still imports one of them is kept out of the catalog by the dangling-import",
    "finding until it is decoupled.",
    "",
    "'compat_modules' lists the single files directly under components/ that",
    "answer what integrations ask a removed layer, without being an",
    "integration: no manifest, no setup. Importing one is not dangling.",
    "",
    "'accepted_transitive' records every domain the closure pulls in that is not",
    "itself a root, with a reviewed reason. A domain reaching the closure without",
    "an entry here fails the check, so new coupling from retained code into an",
    "unreviewed component cannot land silently.",
    "",
    "status values:",
    "  retained       - part of the runtime; expected to stay",
    "  adapter        - coupling lives in a removable platform-adapter file, so",
    "                   the target leaves with that file rather than on its own",
    "  patch_required - genuine coupling that needs a decoupling change first",
    "",
    "'cross_cutting_platforms' are platform files the runtime loads BY NAME via",
    "async_get_platform, never by import, and dispatches to generically. The",
    "edge graph cannot see them, so deleting a provider removes a capability",
    "without breaking an import. They do not grow the closure -- the runtime",
    "requires no particular provider -- but providers outside it are reported",
    "as 'capability at risk'.",
    "",
    "Self-scoped platforms are deliberately absent: config_flow, diagnostics,",
    "application_credentials and config only extend their own integration, so",
    "they leave with it and warning about them would be noise."
  ],
  "roots": {
    "entity_domain_substrate": [
      "alarm_control_panel",
      "binary_sensor",
      "button",
      "calendar",
      "camera",
      "climate",
      "cover",
      "date",
      "datetime",
      "device_tracker",
      "event",
      "fan",
      "geo_location",
      "humidifier",
      "image",
      "image_processing",
      "infrared",
      "lawn_mower",
      "light",
      "lock",
      "media_player",
      "notify",
      "number",
      "radio_frequency",
      "remote",
      "scene",
      "select",
      "sensor",
      "siren",
      "switch",
      "tag",
      "text",
      "time",
      "todo",
      "update",
      "vacuum",
      "valve",
      "water_heater",
      "weather"
    ],
    "retained_integrations": [
      "fronius",
      "hue",
      "matter",
      "miele",
      "modbus",
      "mqtt",
      "shelly"
    ],
    "runtime_infrastructure": [
      "api",
      "application_credentials",
      "auth",
      "bluetooth",
      "config",
      "conversation",
      "dhcp",
      "diagnostics",
      "homeassistant",
      "http",
      "isal",
      "logger",
      "mcp_server",
      "network",
      "persistent_notification",
      "recovery_mode",
      "repairs",
      "ssdp",
      "system_health",
      "system_log",
      "usb",
      "webhook",
      "websocket_api",
      "zeroconf"
    ],
    "device_class_semantics": [
      "air_quality",
      "battery",
      "door",
      "doorbell",
      "garage_door",
      "gate",
      "humidity",
      "illuminance",
      "moisture",
      "motion",
      "occupancy",
      "power",
      "temperature",
      "vibration",
      "window"
    ],
    "aggregation": ["group", "person"],
    "environment": ["sun"],
    "test_fixtures": ["demo", "kitchen_sink"]
  },
  "cross_cutting_platforms": [
    "backup",
    "condition",
    "intent",
    "reproduce_state",
    "significant_change",
    "trigger"
  ],
  "accepted_transitive": {
    "device_automation": {
      "status": "adapter",
      "reason": "Entity domains ship device_action.py/device_trigger.py adapters for Home Assistant device automation.",
      "issue": 19
    },
    "intent": {
      "status": "retained",
      "reason": "Declared dependency of conversation. The overview keeps conversation/intent/LLM primitives because mcp_server requires them.",
      "issue": null
    },
    "llm": {
      "status": "retained",
      "reason": "LLM API substrate behind mcp_server; entity domains expose themselves to it through llm.py.",
      "issue": null
    },
    "manual": {
      "status": "retained",
      "reason": "demo's alarm_control_panel platform builds on the manual alarm panel entity.",
      "issue": null
    },
    "media_source": {
      "status": "retained",
      "reason": "Media browsing and resolution for camera, image and media player entities; camera/media_source.py and image/media_source.py import it at module level.",
      "issue": null
    },
    "stream": {
      "status": "retained",
      "reason": "Camera streaming infrastructure (integration_type: system), imported by camera/__init__.py at module level.",
      "issue": null
    },
    "web_rtc": {
      "status": "retained",
      "reason": "Camera WebRTC ICE infrastructure (integration_type: system), imported by camera/__init__.py at module level.",
      "issue": null
    },
    "zone": {
      "status": "retained",
      "reason": "Declared dependency in device_tracker/manifest.json, and imported from device_tracker/entity.py and legacy.py. Not an adapter: it cannot leave while device_tracker stays.",
      "issue": null
    }
  },
  "excluded": {
    "presentation": [
      "brands",
      "default_config",
      "file_upload",
      "frontend",
      "hardware",
      "image_upload",
      "lovelace",
      "map_tiles",
      "my",
      "onboarding",
      "search"
    ],
    "automation": [
      "automation",
      "blueprint",
      "counter",
      "input_boolean",
      "input_button",
      "input_datetime",
      "input_number",
      "input_select",
      "input_text",
      "schedule",
      "script",
      "template",
      "timer",
      "trace"
    ],
    "history": ["energy", "history", "logbook", "recorder"],
    "voice_and_ai": [
      "ai_task",
      "assist_pipeline",
      "assist_satellite",
      "stt",
      "tts",
      "wake_word"
    ],
    "cloud_and_platform": [
      "alexa",
      "analytics",
      "backup",
      "cloud",
      "google_assistant",
      "hassio",
      "labs"
    ]
  },
  "compat_modules": {
    "automation": "automations_with_entity/automations_with_device return no automations; integrations call them before deprecating an entity.",
    "cloud": "async_active_subscription and async_is_connected are false, creating a cloudhook raises CloudNotConnected; integrations fall back to their local webhook URL.",
    "counter": "DOMAIN only; helpers name it in entity selectors.",
    "default_config": "DOMAIN only; go2rtc sets itself up when default_config is configured, which in ha-lite it never is.",
    "input_boolean": "DOMAIN only; HomeKit and helpers name it among supported domains.",
    "input_button": "DOMAIN and SERVICE_PRESS; HomeKit names it among supported domains.",
    "input_number": "DOMAIN, ATTR_VALUE, CONF_MIN, CONF_MAX, CONF_STEP and SERVICE_SET_VALUE; HomeKit and helpers name it among supported domains.",
    "input_select": "DOMAIN and SERVICE_SELECT_OPTION; HomeKit names it among supported domains.",
    "input_text": "DOMAIN only; bayesian names it in an entity selector.",
    "onboarding": "async_is_onboarded is always true; config flows call it to auto-confirm discoveries during the browser wizard ha-lite does not have.",
    "script": "DOMAIN, CONF_MODE and scripts_with_entity, which returns no scripts."
  }
}
